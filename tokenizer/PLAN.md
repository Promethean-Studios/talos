# tokenizer (Phase 2)
Scope: byte-level BPE/SentencePiece-style tokenizer training for ~128K vocab,
save/load, encode/decode. Not implemented in Phase 1 (vocab_size is configurable
now for later plug-in).
