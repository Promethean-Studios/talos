# inference (Phase 2)
Scope: OpenAI-compatible server, continuous batching, streaming, KV-cache reuse,
sampling/stopping. The Phase 1 KV cache + prefill/decode path is the foundation.
Not implemented in Phase 1.
